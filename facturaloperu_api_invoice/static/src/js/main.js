window.onload = function() {
    // var ugly = document.getElementsByName('api_json')[0];
    // var obj = JSON.parse(ugly);
    // var pretty = JSON.stringify(obj, undefined, 4);
    // document.getElementsByName('api_json p')[0] = pretty;
}
